{% set section_title = title|default('') %}
{% set products_shortcode = products_shortcode|default('') %}
{% set fallback_shortcode = fallback_shortcode|default('') %}
{% set products_html = products_shortcode ? function('do_shortcode', products_shortcode) : '' %}
{# e.g. a category with no featured products of its own falls back to the site-wide featured set #}
{% if not (products_html|trim) and fallback_shortcode %}
	{% set products_html = function('do_shortcode', fallback_shortcode) %}
{% endif %}

{# Nothing to show at all (e.g. no featured items on sale yet): skip the whole section rather than an empty header #}
{% if products_html|trim %}
<section class="rural-product-carousel px-6 pb-20 lg:px-[100px] lg:pb-[180px]">
	<div class="mx-auto flex w-full max-w-[1720px] flex-col items-center gap-8 lg:gap-[60px]">
		<div class="flex w-full flex-wrap items-center justify-between gap-4 lg:gap-8">
			<div class="flex min-w-0 basis-full items-center gap-4 md:basis-auto md:flex-1 lg:gap-[52px]">
				<div class="flex items-center gap-2" aria-hidden="true">
					<span class="h-10 w-1 bg-goldenrod lg:h-[42px]"></span>
					<span class="h-10 w-1 bg-goldenrod lg:h-[42px]"></span>
					<span class="h-10 w-1 bg-goldenrod lg:h-[42px]"></span>
				</div>

				<h2 class="font-heading text-d2-sm uppercase text-birch lg:text-d2 lg:leading-[58px]">{{ section_title }}</h2>
			</div>

			<div class="ml-auto flex flex-none items-center justify-end gap-1 lg:gap-3">
				<button class="rural-product-carousel__nav" type="button" data-carousel-prev aria-label="Previous products" disabled>
					<svg width="22" height="23" viewBox="0 0 22 23" fill="none" xmlns="http://www.w3.org/2000/svg">
<path d="M20.7072 11.4142H2.37382" stroke="#006600" stroke-width="2" stroke-linecap="square" stroke-linejoin="bevel"/>
<path d="M10.7072 21.4142L0.707153 11.4142L10.7072 1.41418" stroke="#006600" stroke-width="2" stroke-linecap="square" stroke-linejoin="bevel"/>
</svg>

				</button>
				<button class="rural-product-carousel__nav" type="button" data-carousel-next aria-label="Next products">
					<svg width="22" height="23" viewBox="0 0 22 23" fill="none" xmlns="http://www.w3.org/2000/svg">
<path d="M1 11.4142H19.3333" stroke="#006600" stroke-width="2" stroke-linecap="square" stroke-linejoin="bevel"/>
<path d="M11 1.41418L21 11.4142L11 21.4142" stroke="#006600" stroke-width="2" stroke-linecap="square" stroke-linejoin="bevel"/>
</svg>

				</button>
			</div>
		</div>

		<div class="rural-product-carousel__viewport w-full">
			{{ products_html|raw }}
		</div>

		<div class="rural-product-carousel__progress" aria-hidden="true">
			<div class="rural-product-carousel__progress-track">
				<div class="rural-product-carousel__progress-bar" data-carousel-progress></div>
			</div>
		</div>
	</div>
</section>
{% endif %}
