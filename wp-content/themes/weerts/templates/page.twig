{% extends 'base.twig' %}

{% block content %}
    <article class="rural-page py-16 lg:py-20">
        <div class="site-width">
            <header class="max-w-full">
                <h1 class="font-heading text-d1-md uppercase text-birch">{{ post.title }}</h1>
            </header>
            <div class="rich-text mt-8 max-w-full">
                {{ post.content }}
            </div>
        </div>
    </article>
{% endblock %}
