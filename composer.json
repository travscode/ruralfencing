{
  "name": "weerts/boilerroom",
  "require": {
    "vlucas/phpdotenv": "^5.5",
    "roots/wp-password-bcrypt": "^1.1"
  },
  "scripts": {
    "pre-install-cmd": [
      "chmod +x setup.sh"
    ],
    "post-install-cmd": [
      "./setup.sh"
    ]
  }
}
